export interface ResetPasswordProps {
  token: string;
};
export interface ResetPasswordPayload {
  email: string | null;
  token: string | null;
  password: string;
  confirmPassword: string;
}

export interface ResetPasswordPayload {
  email: string | null;
  token: string | null;
  password: string;
  confirmPassword: string;
}

export interface ResetPasswordPayload {
  email: string | null;
  token: string | null;
  password: string;
  confirmPassword: string;
}
